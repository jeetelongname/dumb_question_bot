<html>
    <head>
        <title><%= title %></title>
        <link href="https://fonts.googleapis.com/css2?family=Recursive:CASL,MONO@0.75,0.5&display=swap" rel="stylesheet">
        <style>
         body {
             font-family: 'Recursive', monospace;
             font-size: 2em;
         }

         .center {
             position: absolute;
             left: 50%;
             top: 40%;
             transform: translate(-50%, -50%);
             padding: 10px;
         }

         form, input {
             margin: 10px 10px 10px 0;
             font-family: 'Recursive', monospace;
             font-size: 1em;
         }

         footer {
             position: absolute;
             left: 50%;
             top: 90%;
             transform: translate(-50%, -50%);
             padding: 10px;

             font-size: 0.7em;
             color: grey;
         }
        </style>
    </head>
    <body>
        <%= yield %>
    </body>
</html>
